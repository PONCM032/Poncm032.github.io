    //highlight those nefre the hour key grey
    //highlight current hour red
    //highlight future hor green
        //add or remove classes
        var dayHeader =  $("#currentDay");

        dayHeader.append(moment().format('dddd[,] MMMM Do YYYY'));

        var currentTime = moment().hour();

        for(i = 0; i<moment.length; i++) {
            if(moment===col-2[i]){
                //???
            }
        };

        console.log(currentTime);

        var a = {};

        $(".saveBtn").on("click", function (event){
            var saveItem = $(this).parents("\form-control-lg");

            if(saveItem.lenght) {
            a.form-control-lg.append(form-control-lg);
            }
        });

        
